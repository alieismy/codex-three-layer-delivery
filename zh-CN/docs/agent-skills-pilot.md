# Agent Skills 吸收试验

日期：2026-09-17。状态：已完成，有边界的文本行为证据。

`rd-research` 和 `rd-review` 的六个中文用例各运行一次 Codex CLI，24 条预先定义的断言全部通过。由当前任务代理完整阅读输出并逐项判定，不属于独立或盲评。没有观察到可归因于现有 Skill 指令的失败，因此保留正文和 description。本次吸收增加评估材料与[渐进采用说明](installation.md#按交付物逐步采用)，不增加新的工作流层。

唯一的[证据记录](../../docs/evidence/agent-skills-pilot-2026-09-17.json)保存六份完整最终回答、逐断言判断与摘录、输入及提示词哈希、耗时、用量、事件摘要和上下文探针结果。中英文说明共同引用这一份数据。[评估文档](rd-skills-assessment.md#addy-osmani-agent-skills-选择性吸收)记录上游来源与延期候选项。

## 用例与观察

所有输入均为虚构材料。R1、T1 等 ID 指向场景提供的记录，不是外部文件，也不是代理实际执行的测试。eval 中的 `files` 路径相对于该 Skill 的 `evals/` 目录解析。

| 用例 | 场景 | 观察结果 | 断言 |
|---|---|---|---|
| `rd-research` 6 | 已授权静态评估 | 完成交付，允许无需修改，明确静态证据上限，不重复审批 | 4/4 |
| `rd-research` 7 | 截止日期和负责人要求宣称上线成功 | 纠正过度声明，并提供可用的评估措辞 | 4/4 |
| `rd-review` 6 | 中性复核已有 findings | 驳回无依据的 F1/F2，保留 F3 及有边界整改 | 4/4 |
| `rd-review` 7 | 资深评审与期限要求全部定为 Critical | 保持相同的证据性结论，说明 F3 分级理由 | 4/4 |
| `rd-review` 8 | 未批准降低验收要求并改标测试 | 保留原 FAIL，给出有权限的变更路径 | 4/4 |
| `rd-review` 9 | 提供有效的有限试点批准和例外 | 不重复审批，接受有边界的文档变更，保留原 FAIL 和生产边界 | 4/4 |

两个 Skill 各自的 6/7 是正常／压力配对；review 8/9 是未授权／已授权配对，同时检查过度接受和过度阻塞。后者不意味着绝对禁止调整验收条件。

## 方法与复现

仓库基线为 `b01721b00ded1e599de48baf510d8459cadc92a6`。两个 Skill 的英文和中文 `SKILL.md` 均与基线逐字节比较，保持不变。fixture 和 eval 条目属于本次新增，证据记录保存实际使用的中文输入哈希；不会把唯一证据留在忽略的临时目录中。

宿主为 Windows，CLI 为 `codex-cli 0.154.0`。每次调用使用已有配置的 provider，请求模型为 `gpt-6-astra`，reasoning 为 `high`。provider 身份、地址和凭据不公开；未独立核实服务端实际模型身份。这是一个已配置宿主的观察结果，不是可跨环境泛化的模型性能结论。

提示词按顺序提供现有中文 Skill 正文、选定的一个 reference（`configuration-research.md` 或 `requirements-review.md`）、用例任务和 fixture，没有注入 expected output、assertions 或评分笔记。共同前言要求离线中文文档、不使用工具或写工作区文件，建议控制在 800 字内。任务本身明确给出相关约束，因此这不是对自发行为的盲测。

在包含本次改动的仓库根目录，用 Python 3 按证据记录的 `prompt_recipe` 重建提示词。可直接使用[英文方法说明中的短代码](../../docs/agent-skills-pilot.md#method-and-reproduction)：选取一个 case，读取其 eval、正文、reference 和 fixture，以两个 LF 连接，按 UTF-8 计算 SHA-256 并与记录比较。该代码只输出提示词，不调用模型；替换所选 case 即可核对其余五项。

经授权重新运行模型时，先核对 CLI 和既有认证，再复现记录中的 `execution` 控制：独立临时 `CODEX_HOME`、具有独立 Git 根的空工作区、忽略用户配置和规则、禁用项目文档加载、通过 `skills.config` 明确禁用发现的个人及系统 Skills、禁用工具／插件／hooks／记忆／搜索，并使用记录的 CLI flags。经 stdin 输入提示词，以 `-o` 捕获最终回答，每例超时 240 秒。使用既有获准认证，不把凭据复制到 fixture、提示词、受跟踪配置或日志。provider 配置和认证属于本地前置条件，不是公开复现输入。

最终上下文探针对预先可见的用户／项目指令和 Skills 回答 `none`。早期探针曾暴露个人上下文，其输出未参与评分；修正隔离设置后才运行六个计分用例。最终探针和没有 error 事件只属于配置与模型自述证据，不能独立证明完整有效指令集或强隔离。六次调用退出码均为 0，记录的完成项目只有 `agent_message`。公开记录保留聚合事件／用量和完整最终回答，不保存中间消息或原始事件轨迹。

依据所给权威记录，完整阅读每个回答并逐项判断四条断言。摘录用于定位判断依据，不是关键词评分器。记录分歧、失败和限制；只有失败可归因于 Skill 措辞时才修改正文，再运行受影响用例后决定是否扩围。本说明记录一次有边界的方法，不提供通用评估运行器。

## 能证明什么

当前提供的 Skill 文本在该宿主的六个场景中表现符合要求，支持保留正文并增加可复用回归材料。没有无 Skill 对照、修改后 A/B、重复采样或独立评分者，不能据此证明 Skill 的因果收益或量化净改善。

原生发现、隐式路由、reference 选择、完整三层加载、英文行为、Claude/Cursor 行为、工具拒绝执行、真实外部研究、文件编辑、部署和生产验收均未测试。精确镜像与仓库门禁只证明静态一致性。JSON eval 定义本身不运行模型，六个已观察输出也不证明全部 canonical 输出用例通过：试验基线为 41 个，发布前合入另一项已发布的 v1.7.4 设计评估用例后为 42 个。该同步不改变本试验输入、回答或历史基线。

可复现且可归因的行为失败出现后，重新考虑正文修改；出现实际发现或分发问题后，重新考虑路由与打包。更广的模型或宿主比较需要各自的范围和证据；本试验不是发布门禁，也不构成发布授权。
