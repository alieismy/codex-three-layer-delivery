---
description: Gotchas 坑点记录机制与完成验证：失败模式记录、3 次规则、安全底线
alwaysApply: true
---

# Gotchas 坑点记录与完成验证

## Gotchas 坑点记录

> 每次 AI 或团队成员犯错时记录失败模式，长期积累成为信噪比最高的工程知识。

### 记录结构

每条 Gotcha 包含四个要素：

```
### YYYY-MM-DD: 简短标题
- **问题**：发生了什么
- **表现**：怎么发现的（评审意见、验证失败、线上问题等）
- **修复**：怎么解决的
- **预防**：怎么避免再犯（加入规则/检查清单/自动化）
```

### 管理原则

- **3 次规则**：同一类坑点出现 3 次以上 → 提升为正式规则（写入 `.cursor/rules/` 或对应 Skill）
- **30 天归档**：超过 30 天未再出现的问题 → 移到归档区，减少噪音
- **Skill 关联**：文档评审或标准评审中发现的典型问题应同步记录为 Gotcha
- **存储位置**：项目根目录 `GOTCHAS.md` 或 `.cursor/rules/` 中的专门规则文件

## 完成与验证

没有验证，不要直接说"完成了"。最终汇报至少区分：

- 已完成
- 已验证
- 未验证
- 临时妥协
- 风险与后续建议

文档或配置任务完成前，优先运行项目已配置的验证脚本、格式检查或一致性检查。若项目未配置相关命令，必须明确说明"未发现已配置的验证命令"，不得用"应该可以"替代验证结果。

### 安全底线

- 不硬编码 secrets / token / API keys
- 不把未验证的设计前提写成确定事实
- 不把未验证的内容说成确定事实
- 不将 demo-only 或示例性内容表述为生产可用
- 提交前检查本次 staged changes 中是否包含疑似 secrets、token、私钥、连接串或 `.env` 内容；发现后停止并报告。
