---
description: 当记录重复失败模式，或验证并汇报文档、规则或配置变更时使用。
alwaysApply: false
---

# 失败模式与验证

## 失败模式治理

- 先完成当前任务；只有证据表明某失败模式可复用且重复发生会造成重大风险时，才记录它。
- 使用项目指定的唯一权威日志。写入前先搜索，不创建竞争日志，也不重复既有规则、Skill、测试、hook、Schema 或 Validator。
- 有用的记录应包含问题、可观察症状、原因或不确定性、纠正、预防、适用边界和证据。
- 将记录提升为正式规则或 Skill 前，先提出最小收紧方案，并取得用户或授权责任人批准。能直接强制不变量时，优先使用可执行控制。
- 失败模式不再适用、适用边界消失，或已由可执行控制替代时，应归档或删除记录。

## 验证纪律

1. 定义预期行为和证据层级。
2. 执行最接近的已有检查。
3. 扩大范围前，先判断失败由当前变更还是既有状态引入。
4. 汇报改了什么、哪些检查通过、哪些内容未验证和剩余风险。

保持 diff 聚焦，保留无关工作，完成前重读已改文件，并检查完整 diff 是否存在断裂引用、意外重复、格式噪音、本机专用路径或疑似密钥。
