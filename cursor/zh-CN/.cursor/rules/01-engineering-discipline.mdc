---
description: 文档交付纪律：先读来源材料、最小改动、保留意图、将文档任务转化为可验证目标。
alwaysApply: true
---

# 文档交付纪律

## 写作前先想清楚

- 当假设会影响需求、可研、设计、标准或评审结论时，必须显式说明。
- 先读目标文档，再读上游需求、可研报告、技术方案、标准、术语表、评审意见和约束。
- 结构化前保留原始文本；不得静默改写用户意图、条款原文或干系人决策。
- 如果影响文档结构、术语、效力或追踪关系，先给出简短计划。
- 多种合理做法影响不同时，给出推荐并说明理由。

## 最小改动原则

- 优先最小必要文档改动。
- 复用既有模板、术语、编号、评审表、目录结构和文档约定。
- 不因为未来可能需要而增加章节或流程负担。
- 不格式化无关内容。
- 清理由本次改动造成的不一致；已有无关问题不处理。

## 目标驱动执行

将模糊文档任务转化为可验证目标：

| 请求 | 可验证目标 |
|------|------------|
| 分析需求 | PRD/SRS 条目包含优先级、假设、排除项和验收标准 |
| 评估可行性 | 可行 / 有条件可行 / 不可行结论包含依据、条件和风险 |
| 写方案 | 候选方案比较和推荐方案依据 |
| 写详细设计 | 接口、数据、流程、错误、安全和未决决策 |
| 评审文档 | 分级问题包含依据、影响和建议 |
| 评审标准 | 引用具体条款，必要时给出规范性替换文本 |

运行验证命令前，先检查项目已有入口，如 `scripts/`、`package.json`、`Makefile`、`justfile` 或文档专用工具。

## 何时直接做 / 何时先问

| 判断 | 行为 |
|------|------|
| 指令明确，改动可控 | 直接做 |
| 多种合理做法存在实质取舍 | 推荐并提问 |
| 模糊点影响范围、成本、合规、风险或架构 | 复述并澄清 |
| 破坏性操作或权威基线变更 | 先问 |
| 可行性不清 | 先说明风险和限制 |
| 研究涉及不确定或易漂移领域 | 先核实 |

## 配置变更

- 配置是可执行行为，应谨慎处理。
- 不把 Codex-only 或 Claude-only 配置语法复制到 Cursor 文件。
- Cursor 项目规则使用 `.cursor/rules/*.md` 或 `.cursor/rules/*.mdc`。
- Cursor 项目 Skills 使用 `.cursor/skills/rd-*/SKILL.md`。
- Cursor MCP 示例保持显式选择启用；验证 `.cursor/mcp.json`、凭据和 Cursor MCP 状态前，不假定服务器已启用。
