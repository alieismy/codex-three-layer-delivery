---
description: 当创建或维护 Cursor 项目规则、模块规则、Skills 或长期项目指引时使用。
alwaysApply: false
---

# 规范注入（Spec Injection）

项目级规范存储在仓库中，使 Cursor Agent 能够跨会话加载一致的项目约定。

## 规范层级

| 层级 | 载体 | 加载方式 |
|------|------|---------|
| 用户全局 | Cursor User Rules | 从 Cursor 设置中应用 |
| 项目规范 | `.cursor/rules/*.mdc` | 项目范围规则 |
| 模块规范 | 嵌套 `.cursor/rules/` 或 `.mdc` frontmatter 中的 `globs` | 作用于匹配路径 |
| 任务能力 | `.cursor/skills/rd-*/SKILL.md` | 相关时由 Cursor Agent 调用 |
| 互操作后备 | `AGENTS.md` | 适用时的普通 Markdown 替代 |

## 规则编写

- 规则应聚焦、可执行，篇幅不超过表达不变量所需的程度。
- 项目规则必须使用 `.mdc`；按适用范围在 frontmatter 中声明 `description`、`globs` 和 `alwaysApply`。
- 将大型概念拆分为可组合规则，避免单个规则覆盖过宽。
- 优先写具体约束、脚本、路径和示例，避免泛泛偏好。
- 不在规则中存储 secrets、私有 URL 或个人凭据。

## 规范检查要求

执行需求、可研、设计、标准或评审工作前：

1. 检查 `.cursor/rules/` 中的项目约定。
2. 检查 `.cursor/skills/` 中的相关 Skill。
3. 创建新规则前，检查既有模板、术语表、评审表、标准引用和已批准决策。
4. 只有稳定、可复用且经用户或授权责任人批准的项目决策，才持久化为长期指引。

## 规范维护

- `.cursor/rules/` 用于 agent 行为和项目约束。
- `README.md` 面向人类，说明项目是什么、为什么存在、如何开始。
- 避免在规则和 README 之间重复同一段内容。
- 新增规则前先搜索是否已有等价规则，优先收紧已有规则而不是重复新增。
- 只有当长期指引稳定、可复用、经用户或授权责任人批准，并可追溯到明确项目要求、已观察到的失败模式或重大项目风险时，才新增或修改；同时说明如何评审或验证符合性。
- Skill frontmatter 的 `description` 是触发摘要，应以触发条件开头，并保持简短。
