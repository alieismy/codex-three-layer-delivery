---
description: 上下文管理与会话策略：上下文保护、会话刷新、两次失败规则、配置迭代
alwaysApply: true
---

# 上下文管理与会话策略

## 上下文保护

- 实施计划中包含**完整文件路径**（即使上下文丢失也能重新定位）
- 复杂多步任务开始前，先摘要当前上下文中的关键约束
- 关键决策持久化到 `.cursor/rules/` 规则文件中

## 会话刷新策略

大型任务推荐拆分为多个独立会话：

| 会话 | 目标 | 持久化 |
|------|------|--------|
| 会话 1 | 理解代码库结构和约束 | 关键发现写入 `.cursor/rules/` 规则 |
| 会话 2 | 基于规则实现功能 | 代码提交到 Git |
| 会话 3 | 独立验证和测试 | 测试结果记录 |

## 何时开新会话

- 回答质量明显下降（重复之前的错误、遗忘关键约束）
- **两次失败规则**：同一问题连续两次修复失败 → 开新会话 + `git reset --hard`，从干净上下文重新开始
- 任务类型切换（如从需求分析切到代码实现）
- 上下文被压缩 2 次以上

## 配置迭代原则

每 3-6 个月或大版本模型发布后做一次配置 review：

- 区分两类规则：**弥补模型局限**的规则（应随模型升级重新评估） vs **项目固有约定**的规则（长期有效）
- 过时的限制性规则会约束新模型能力
- 清理过期配置和写新配置一样重要
