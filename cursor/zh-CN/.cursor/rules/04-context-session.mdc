---
description: 上下文管理、会话刷新、文档地图和搜索卫生。
alwaysApply: true
---

# 上下文与会话管理

## 上下文健康

- 计划、评审报告和最终汇报中引用本地文件时使用完整路径。
- 复杂多步任务开始前先摘要关键约束。
- 任务在需求、可研、研究、方案、详细设计、标准、专业写作、评审和交付编排之间切换时，刷新上下文。
- 如果回答质量下降或同一问题连续失败两次，先暂停并重新评估上下文。
- 仅在权威来源、修订标识和适用范围仍明确时，复用已经检查的稳定项目上下文。
- 开始实质工作前，使用分支与 HEAD、工作区状态、变更路径和相关来源修订等适用的低成本变化探针界定刷新范围；默认不重复扫描整个仓库。
- 发生修订变化、外部编辑、新证据、上下文压缩、任务转向或观察冲突时，重新读取受影响或决策关键的来源。未检查适用的变化探针时，不得声称既有上下文仍是当前事实。
- 对复杂或高影响工作，如果有助于评审，应在第一个有意义的检查点简要说明复用了哪些基线、刷新了哪些动态证据；不得给简单任务强加固定的上下文核算格式。

## 任务状态边界

- 当前目标、进度、分支或提交快照、测试结果、已知问题、失败方案和下一动作应保存在任务计划或交付记录中，不写入本文件。本文件可以规定何时、从何处读取这些状态，但必须只维护长期稳定的项目规则。

## 会话策略

大型任务拆分为可独立验证的会话：

| 会话 | 目标 | 持久化 |
|------|------|--------|
| 1 | 理解文档、范围和约束 | 笔记、规则或文档地图 |
| 2 | 起草或修订文档内容 | 可追踪文档变更 |
| 3 | 独立评审和验证 | 评审意见和验证记录 |

## 文档地图

大型文档仓库建议在项目根目录维护轻量 `DOCUMENTS.md`：

```text
# 文档地图
## 需求
- docs/prd/              - PRD 和 SRS
## 可研
- docs/feasibility/      - 可研报告和立项材料
## 设计
- docs/design/           - 技术方案、概要设计、详细设计
## 标准
- docs/standards/        - 标准草案、引用资料、评审意见
## 不直接编辑
- published/             - 已批准基线
```

地图只描述高层结构；详细约束放到被约束文档附近。

## 噪音排除

除非是评审目标，否则常规搜索范围排除生成文件、导出件、构建产物、第三方内容和临时文件。

## 配置迭代

每 3-6 个月或模型大版本发布后复核 agent 配置。区分弥补模型局限的规则和长期项目约定。
